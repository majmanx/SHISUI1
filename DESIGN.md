# 石髓 SHISUI · 设计文档

> 大理石 × 液态金属 × 毒液。把古筝、二胡、竹笛、管子/唢呐、弦乐群"插上电"的浏览器合成器。

## 1. 主题与视觉

| 元素 | 实现 |
| --- | --- |
| 大理石 | SVG `feTurbulence` 分形噪声 → 取 **等值线**（噪声值 ≈ 0.5 的一圈）染深 = 天然纹路；叠一层低频噪声做冷暖色调 |
| 金脉 | 另一组噪声的等值线，只保留极窄一段值，填金色；再复制一层加 `blur` + `screen` 混合，`opacity` 慢速脉动 = "光从金脉里迸出" |
| 光 | 三个大半径径向渐变光斑，`screen` 混合，缓慢漂移 |
| 液态金属 | 旋钮：径向渐变镀铬体 + 高光椭圆 + 金色斜向渐变外环；按钮：上白下灰的金属压边；面板：半透明奶白玻璃 + 顶部金线 |
| 毒液模式 | CSS 变量整体切换：黑曜石大理石（暗色等值线）+ 酸绿/紫罗兰脉络 + 黑铬旋钮；乐器内核同时开启反馈 FM、波折叠、碎裂、流淌漂移 |

动效只用 `opacity` / `transform`，噪声滤镜是静态栅格，一次绘制后由合成器缓存；"低动效"动作可关掉脉动。

## 2. 声音架构

```
键盘 / MIDI / 琶音 / 代码 / 衰变事件
   │
   ▼
[合成器 Worklet]  古筝(KS + 三角位移激励 + 色散 + 摇指/按滑/止音)
                  二胡(弓弦摩擦表 + 双延迟线 + 琴筒共鸣)
                  竹笛(射流延迟 + 管体延迟 + x³−x 射流非线性 + 笛膜嗡鸣)
                  管子/唢呐(簧片表 + 半波长闭管 + 喇叭口折叠)
                  弦乐群(3×PolyBLEP 锯齿失谐 + 方波混合)
                  → 拾音器(非对称 tanh) + 合成层 + 次低音 + 毒液(反馈FM/折叠/漂移)
   ▼
[滤波 Biquad + LFO detune]
   ▼
[放大器 Worklet]  碎裂(采样保持+量化) → 二极管削波(线性区 + 对数压缩，锗/硅非对称阈值, 2× 过采样)
                  → 电子管(tanh 三极管曲线 + 栅偏压 + 电源下垂包络 + 推挽交越死区 + 输出变压器 LP + DC 阻断)
                  → 箱体(3 段 biquad：琴身 / 吉他箱 / 毒液箱) → 50Hz 哼声 / 底噪
   ▼
合唱(双延迟反相 LFO) → 乒乓延迟(反馈路径低通) → 石厅混响(生成式脉冲响应 + 早期反射) → 压缩 → 主音量 → 限幅 → 分析器
```

所有 DSP 写在 `js/dsp.worklet.js` 的一个函数里，用 `Function.prototype.toString` 转成 data: URL 载入 AudioWorklet，所以**双击 index.html 就能用**，也能在 Node 里做数值测试。

## 3. 随机接口

| 源 | 机制 |
| --- | --- |
| 真随机 | `crypto.getRandomValues` 32 位字 |
| 圆周率 | Gibbons 无界螺旋算法（BigInt），按需即时生成数字流，每次取 4 位 → 0..1；面板显示当前消耗位置 |
| 碳-14 | 泊松过程模拟：N 个原子、λ = ln2/5730 年、可调"年/秒"。每帧采样衰变数 k；用**随机化概率积分变换**把 k 映射成严格均匀的 0..1（条件于至少一次衰变，保证事件值均匀）；样本会真的耗尽（可看到剩余原子数、半衰期计数、活度） |

- 四个接口 R1–R4：手动刷新（按钮/空格）、自动刷新（Hz）、平滑（slew）。碳-14 源下每次衰变轮流刷新一个接口。
- 衰变事件同时是：`DECAY` 调制脉冲、盖革咔嗒声、按概率触发五声音阶音符、`arp.mode = c14` 时推进琶音、代码台 `onDecay` 回调。
- "惊喜"随机化整套音色：每个参数有自己的**安全随机区间**（`rnd:[lo,hi]`），幅度旋钮控制偏离度，右键锁定的旋钮跳过。随机数来自当前选择的源。

## 4. 控制方式

- **模拟**：旋钮（拖/滚轮/Shift 微调/双击复位/右键锁）、XY 板、屏幕键盘（点击位置决定力度、可滑奏）、电脑键盘、Web MIDI（调制轮 = `WHEEL` 源，CC74 = 截止，弯音）。
- **调制矩阵**：点源芯片或接口球 → 点任意旋钮即接线；旋钮外圈的彩色弧与光点实时显示被调制值；矩阵里改深浅/删除。
- **代码**：`note / seq / every / set / get / mod / pi / c14 / rnd / onDecay / venom / inst / preset`，Ctrl+Enter 运行，基于 AudioContext 时钟的前瞻调度。
- **JSON**：音色以"与默认值的差"导出，可编辑后应用、复制分享。

## 5. 借鉴与改进（对标分析）

### 极简类（如单屏合成器、Operator 式）
- 优点：打开即玩、每个控件都有意义。
- 痛点：想深挖时没有深度；隐藏功能靠说明书。
- 我们：三层界面 **玩 / 塑 / 深**，同一台机器逐层展开；"玩"层只有 4 个宏 + XY + 随机接口 + 乐器瓷砖。

### 搞笑/玩具类（香蕉贝斯之类）
- 优点：有个性、让人想按；名字和外观带来情绪。
- 痛点：一招鲜，玩十分钟就腻；几乎没有可编辑性；音色无法迁移到正经作品。
- 我们：保留"毒液"开关这种一键变身的爽感（连界面都变黑曜石）和"惊喜"骰子，但底下是完整的建模与放大链，且每个预设都能继续雕。

### 全能类（Serum 式波表旗舰）
- 优点：可视化调制、拖拽接线、巨大的预设库、效果链完整。
- 常见用户痛点（来自论坛/评测的高频反馈）：
  1. CPU 重、初学者被几十个模块吓退 → 我们默认 8 复音、单一 Worklet，"玩"层零学习成本。
  2. 预设浏览器慢、找不到参数 → `⌘K` 全局查找任意参数/预设/动作，命中后自动切层并闪烁定位；底部信息栏悬停即解释。
  3. 随机化要么没有要么不"乐"→ 每参数安全区间 + 幅度 + 锁定 + 三种可选随机源。
  4. 调制量看不见 → 旋钮外圈弧线 + 光点直接画出实际值。
  5. 撤销/AB 弱 → 撤销栈 80 步、A/B 槽、JSON 差量导出。
  6. UI 小、字看不清 → 相对单位、可缩放布局、平板也能拖旋钮（pointer events + `touch-action:none`）。

### 我们的不同点
- 内核是**中国弦管乐器的物理建模**而非波表：古筝的摇指与按滑、二胡的弓压与松香噪、竹笛的笛膜与超吹、管子/唢呐的簧片硬度与喇叭口，都是真实演奏语汇的参数。
- 随机不是"随机"而是**三种可听可看的物理/数学过程**：π 的数字、碳-14 的衰变、系统真随机。
- 放大链按**真实电路**分级：拾音器 → 二极管 → 电子管（含电源下垂、交越失真、哼声）→ 箱体，而不是一个"drive"旋钮。

## 6. 录音与声音槽

- 主输出（限幅之后）接一个录音 Worklet，把 PCM 分块送回主线程；停止时拼成 `AudioBuffer`，可导出 16-bit WAV，无需 DAW。
- 8 个声音槽：录音自动进入下一个空槽，也可导入 wav/mp3/ogg。每个槽有播放/循环/音量/速度（变调）；可选"过效果链"（槽 → 滤波 → 放大器 → 效果）或直入总线。
- 叠录工作流：让几个槽循环，再弹奏并录音，新录音包含旧素材 → 用旧声音混出新音色。槽只驻留内存；想保留就导出 WAV。

## 7. 持久化

- 每次改动 400 ms 后自动把当前音色存入 `localStorage`，下次打开自动恢复。
- "存预设"把差量 JSON 存入本地"我的预设"，出现在预设列表与 ⌘K 查找里，可删除。

## 8. 电脑键盘

FL Studio 式双排：`Z…M` 低八度（`S D G H J` 黑键），`Q…]` 高八度（数字键黑键），`,` `.` 移八度。屏幕键盘 58 键（C2–A6），电脑键位对应的琴键上有字母提示与金色下划线。

## 9. 字体

标题 / 面板名 / 乐器瓷砖 / 宏名用毛笔楷书 **Ma Shan Zheng**（笔锋明显、现代人可读），正文与旋钮标签用 **ZCOOL XiaoWei**（碑刻宋味、清晰），回退到系统楷体 / 宋体。

## 10. 文件结构

```
index.html          页面骨架 + SVG 渐变/滤镜 + 帮助
css/style.css       主题（大理石 / 毒液两套变量）
js/dsp.worklet.js   AudioWorklet DSP（合成器 + 放大器）
js/params.js        参数注册表（范围/曲线/格式/提示/随机区间）
js/random.js        真随机 / π 螺旋 / 碳-14 衰变 / 随机总线
js/presets.js       出厂预设 + 默认宏接线
js/engine.js        音频图（滤波、合唱、延迟、混响、压缩、MIDI）
js/ui.js            旋钮 / 选择器 / 开关 / 键盘 / XY / 示波器 / 查找
js/code.js          调度器 / 琶音器 / 代码台
js/app.js           状态、面板、调制循环、随机化、预设、撤销、A/B
```
